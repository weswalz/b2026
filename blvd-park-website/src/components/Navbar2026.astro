---
// src/components/Navbar2026.astro
// 2026 Redesign - Premium navbar with fullscreen mobile flyout
import MobileMenu2026 from './MobileMenu2026.astro';
---

<header class="navbar-2026 fixed top-0 left-0 right-0 z-50">
  <!-- Glass backdrop -->
  <div class="navbar-glass absolute inset-0 transition-all duration-500"></div>

  <nav class="container mx-auto px-6 relative z-10">
    <div class="flex items-center justify-between h-20">

      <!-- Logo -->
      <a href="/" class="navbar-logo flex items-center group">
        <img
          src="/images/logos/blvd-park-logo.webp"
          alt="BLVD Park"
          data-cms-key="nav.logo_src"
          data-cms-attr="src:nav.logo_src,alt:nav.logo_alt"
          class="logo-img h-10 w-auto transition-all duration-300 group-hover:scale-105"
        />
      </a>

      <!-- Desktop Nav -->
      <div class="hidden lg:flex items-center gap-1">
        <a
          href="/menu"
          class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300"
          data-cms-key="nav.links.0.label"
          data-cms-attr="href:nav.links.0.href"
        >
          Menu
        </a>
        <a
          href="/#pickleball"
          class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300"
          data-cms-key="nav.links.1.label"
          data-cms-attr="href:nav.links.1.href"
        >
          Pickleball
        </a>
        <a
          href="/#events"
          class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300"
          data-cms-key="nav.links.2.label"
          data-cms-attr="href:nav.links.2.href"
        >
          Events
        </a>
        <a
          href="/private-events"
          class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300"
        >
          Private Events
        </a>
        <a
          href="/#hours-location"
          class="nav-link px-4 py-2 rounded-full text-sm font-medium transition-all duration-300"
          data-cms-key="nav.links.3.label"
          data-cms-attr="href:nav.links.3.href"
        >
          Hours
        </a>
      </div>

      <!-- Right Side -->
      <div class="flex items-center gap-4">
        <!-- CTA Button -->
        <a
          href="/book"
          class="nav-cta hidden sm:flex items-center gap-2 px-5 py-2.5 rounded-full text-sm font-semibold transition-all duration-300 hover:scale-105"
          data-cms-attr="href:nav.cta.href"
        >
          <span data-cms-key="nav.cta.label">Reserve</span>
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
        </a>

        <!-- Mobile Menu Toggle -->
        <button
          id="mobile-toggle"
          class="mobile-toggle lg:hidden w-12 h-12 rounded-2xl flex items-center justify-center transition-all duration-300"
          aria-label="Toggle menu"
        >
          <div class="hamburger-icon w-6 h-5 relative">
            <span class="hamburger-line line-1"></span>
            <span class="hamburger-line line-2"></span>
            <span class="hamburger-line line-3"></span>
          </div>
        </button>
      </div>

    </div>
  </nav>
</header>

<MobileMenu2026 />

<style>
  /* ===== NAVBAR BASE ===== */
  .navbar-2026 {
    --nav-bg: transparent;
    --nav-text: white;
    --nav-cta-bg: white;
    --nav-cta-text: #0F3D22;
    --blvd-green: #1A5F36;
    --blvd-green-bright: #22C55E;
    --blvd-gold: #C9A962;
  }

  .navbar-glass {
    background: var(--nav-bg);
    backdrop-filter: blur(0px);
  }

  .logo-text,
  .nav-link {
    color: var(--nav-text);
  }

  .nav-link:hover {
    background: rgba(255, 255, 255, 0.1);
  }

  .nav-cta {
    background: var(--nav-cta-bg);
    color: var(--nav-cta-text);
  }

  /* Hamburger Button */
  .mobile-toggle {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .hamburger-icon {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .hamburger-line {
    display: block;
    position: absolute;
    height: 2px;
    width: 100%;
    background: white;
    border-radius: 2px;
    transition: all 0.4s cubic-bezier(0.68, -0.6, 0.32, 1.6);
  }

  .hamburger-line.line-1 { top: 0; }
  .hamburger-line.line-2 { top: 50%; transform: translateY(-50%); }
  .hamburger-line.line-3 { bottom: 0; }

  /* Hamburger X Animation */
  .navbar-2026.menu-open .hamburger-line.line-1 {
    top: 50%;
    transform: translateY(-50%) rotate(45deg);
  }

  .navbar-2026.menu-open .hamburger-line.line-2 {
    opacity: 0;
    transform: translateX(-20px);
  }

  .navbar-2026.menu-open .hamburger-line.line-3 {
    bottom: 50%;
    transform: translateY(50%) rotate(-45deg);
  }

  /* Scrolled State */
  .navbar-2026.scrolled {
    --nav-bg: rgba(15, 61, 34, 0.9);
  }

  .navbar-2026.scrolled .navbar-glass {
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .logo-img {
    filter: brightness(0) invert(1);
  }

  /* Nav Link Underline */
  .nav-link {
    position: relative;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 6px;
    left: 50%;
    width: 0;
    height: 2px;
    background: var(--blvd-gold);
    transform: translateX(-50%);
    transition: width 0.3s cubic-bezier(0.19, 1, 0.22, 1);
    border-radius: 1px;
  }

  .nav-link:hover::after {
    width: calc(100% - 32px);
  }

  .nav-cta:hover {
    box-shadow: 0 4px 20px rgba(255, 255, 255, 0.2);
  }

  .navbar-logo:hover .logo-mark {
    background: var(--blvd-green-bright) !important;
  }
</style>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    var navbar = document.querySelector('.navbar-2026');
    var mobileToggle = document.getElementById('mobile-toggle');
    var mobileMenu = document.getElementById('mobile-menu');
    var mobileClose = document.getElementById('mobile-close');
    var mobileLinks = document.querySelectorAll('.mobile-nav-link');

    // Scroll detection
    function handleScroll() {
      if (window.scrollY > 50) {
        navbar && navbar.classList.add('scrolled');
      } else {
        navbar && navbar.classList.remove('scrolled');
      }
    }

    window.addEventListener('scroll', handleScroll, { passive: true });
    handleScroll();

    function openMenu() {
      navbar && navbar.classList.add('menu-open');
      mobileMenu && mobileMenu.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeMenu() {
      navbar && navbar.classList.remove('menu-open');
      mobileMenu && mobileMenu.classList.remove('open');
      document.body.style.overflow = '';
    }

    mobileToggle && mobileToggle.addEventListener('click', function() {
      if (mobileMenu && mobileMenu.classList.contains('open')) {
        closeMenu();
      } else {
        openMenu();
      }
    });

    mobileClose && mobileClose.addEventListener('click', closeMenu);

    mobileLinks.forEach(function(link) {
      link.addEventListener('click', function() {
        closeMenu();
      });
    });

    var ctaLinks = document.querySelectorAll('.mobile-cta-primary, .mobile-cta-secondary');
    ctaLinks.forEach(function(link) {
      link.addEventListener('click', closeMenu);
    });

    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && mobileMenu && mobileMenu.classList.contains('open')) {
        closeMenu();
      }
    });
  });
</script>
