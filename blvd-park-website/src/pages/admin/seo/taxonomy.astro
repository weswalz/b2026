---
export const prerender = true;
// Admin entities + taxonomy management
import AdminLayout from '../../../layouts/AdminLayout.astro';
import AdminPage from '../../../components/admin/AdminPage';
---

<AdminLayout title="Entities & Taxonomy">
  <AdminPage client:only="react" currentPath={Astro.url.pathname} page="seo-taxonomy" />
</AdminLayout>
